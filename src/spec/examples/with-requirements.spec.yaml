version: '2'
name: Echo contract
description: Each behavior is a requirement; the verifier records a result against its stable ID.
target:
  type: cli
  binary: echo
areas:
  - id: output
    name: Text output
    behaviors:
      - id: echoes-argument
        description:
          Running echo with the argument hello exits successfully and prints hello followed by a
          newline.
