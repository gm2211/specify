node_modules/
dist/
.env
.auth/
*.log

# Local evidence bundles and legacy runtime data (never removed by migration)
.specify/

# Legacy paths (before consolidation into .specify/)
captures/
agent-runs/
cli-report/

# Generated review files
*.review.html

# Dolt database files (added by bd init)
.dolt/
*.db

# Beads / Dolt files (added by bd init)
.beads-credential-key

# Terraform local state
deploy/terraform/**/.terraform/
deploy/terraform/**/.terraform.lock.hcl
deploy/terraform/**/terraform.tfstate*
deploy/terraform/**/*.auto.tfvars

.last-update-check

.beads/issues.jsonl
.beads/interactions.jsonl
.beads/export-state.json
