# Spec Verification Report

**Spec:** `{{ spec_path }}`
**Target:** {{ target.type }} — `{{ target.binary or target.base_url }}`
**Run:** {{ run_id }} ({{ started_at }})
**Duration:** {{ duration_s }}s

## Summary

| Status   | Count |
|----------|-------|
| Passed   | {{ counts.passed }} |
| Failed   | {{ counts.failed }} |
| Skipped  | {{ counts.skipped }} |
| **Total**| {{ counts.total }} |

{% if counts.failed > 0 %}
## Failures

{% for f in failures %}
### `{{ f.area_id }} / {{ f.behavior_id }}`

**Claim:** {{ f.description }}

**Cause:** {{ f.cause }}

<details>
<summary>Evidence</summary>

```
{{ f.evidence }}
```
</details>

{% endfor %}
{% endif %}

## All behaviors

{% for r in results %}
- {{ r.icon }} `{{ r.area_id }} / {{ r.behavior_id }}` — {{ r.description }}
{% endfor %}
